export type ExperienceEntry = {
  position: string
  company: string
  companyLink: string
  time: string
  address: string
  description: string
  responsibilities: string[]
}

export type Skills = {
  frontend: string[]
  backend: string[]
  other: string[]
}
